import { type FormEvent, useMemo, useState } from 'react';
import { Link, useLocation, useParams } from 'wouter';
import { ArrowLeft, ArrowRight, BriefcaseBusiness, Check, CheckCircle2, ChevronDown, Clock3, MapPin, MessageCircle, Search, ShieldCheck, Sparkles, Star, Wrench, X } from 'lucide-react';
import { Avatar, ButtonLink, SearchBar, SectionEyebrow, Shell } from '@/components/skilllink-shell';
import { createJob, getFundis, getJobs, getSession, getUsers, saveSession, saveUsers, services, toggleFundiAvailability, updateJobStatus, type Fundi, type Job, type JobStatus, type Role, type User } from '@/lib/store';

function Rating({ rating, count }: { rating: number; count?: number }) { return <span className="inline-flex items-center gap-1 text-sm font-semibold"><Star size={14} fill="currentColor" className="text-[#e6a435]" />{rating.toFixed(1)}{count !== undefined && <span className="font-normal text-muted-foreground">({count})</span>}</span>; }
function VerifiedBadge() { return <span className="inline-flex items-center gap-1 rounded-full bg-secondary px-2.5 py-1 text-[11px] font-bold text-primary"><CheckCircle2 size={13} />Identity-checked</span>; }

export function HomePage() {
  const [faq, setFaq] = useState<number | null>(0);
  return <Shell>
    <section className="container-wide grid items-center gap-12 pb-16 pt-14 md:grid-cols-[1fr_.92fr] md:gap-16 md:pb-24 md:pt-20">
      <div className="rise-in"><div className="mb-5"><span className="inline-flex items-center gap-1.5 rounded-full bg-secondary px-3 py-1.5 text-[11px] font-bold text-primary"><CheckCircle2 size={13} />Identity-checked fundis</span></div><h1 className="font-display max-w-[580px] text-[clamp(3rem,7vw,5.6rem)] leading-[.92] tracking-[-.06em] text-foreground">Find Trusted <span className="text-primary">Fundis</span><br />Near You</h1><p className="mt-6 max-w-[510px] text-[17px] leading-7 text-muted-foreground">Book skilled professionals for repairs, maintenance and everyday jobs across Kenya.</p><div className="mt-8 max-w-[560px]"><SearchBar /></div><div className="mt-5 flex items-center gap-4 text-xs text-muted-foreground"><span className="flex items-center gap-1.5"><Check size={13} className="text-primary" />Verified professionals</span><span className="flex items-center gap-1.5"><Check size={13} className="text-primary" />Clear communication</span></div></div>
      <div className="hero-art rise-in rise-delay-2 relative mx-auto aspect-[.88] w-full max-w-[510px] rounded-[2rem] shadow-[var(--shadow-soft)]"><div className="absolute left-6 top-6 z-10 rounded-2xl border border-white/60 bg-white/75 px-4 py-3 shadow-lg backdrop-blur"><p className="text-[10px] font-bold uppercase tracking-[.12em] text-muted-foreground">Today in Nairobi</p><p className="mt-1 flex items-center gap-2 text-sm font-bold"><span className="h-2 w-2 rounded-full bg-primary" /> 128 fundis available</p></div><div className="worker-head" /><div className="worker-silhouette" /><div className="worker-tool" /><div className="absolute bottom-6 left-6 z-10 flex items-center gap-2 rounded-2xl border border-white/60 bg-white/80 px-3 py-2.5 shadow-lg backdrop-blur"><span className="grid h-8 w-8 place-items-center rounded-full bg-primary text-white"><ShieldCheck size={16} /></span><div><p className="text-[11px] font-bold">Work you can trust</p><p className="text-[10px] text-muted-foreground">Checked before they join</p></div></div></div>
    </section>
    <section className="border-y border-border/60 bg-[#f4f8f3] py-14 md:py-16"><div className="container-wide"><div className="flex items-end justify-between gap-6"><div><SectionEyebrow>Start here</SectionEyebrow><h2 className="font-display text-3xl tracking-[-.04em] md:text-4xl">Popular services</h2><p className="mt-2 text-sm text-muted-foreground">Browse fundis by the kind of work you need done.</p></div><Link href="/search" className="hidden items-center gap-2 text-sm font-bold text-primary sm:flex" data-testid="link-see-all-services">See all services <ArrowRight size={15} /></Link></div><div className="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{services.map((item, index) => <Link key={item.name} href={`/search?service=${encodeURIComponent(item.name)}`} className="group rounded-2xl border border-border/80 bg-card p-4 transition-all hover:-translate-y-1 hover:border-primary/40 hover:shadow-[var(--shadow-card)]" data-testid={`card-service-${index}`}><span className="mb-5 grid h-9 w-9 place-items-center rounded-xl bg-secondary text-primary"><Wrench size={17} /></span><p className="text-sm font-bold">{item.name}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{item.detail}</p></Link>)}</div></div></section>
    <section className="container-wide py-20 md:py-28"><div className="mx-auto max-w-2xl text-center"><SectionEyebrow>Simple from the start</SectionEyebrow><h2 className="font-display text-4xl tracking-[-.05em] md:text-5xl">Getting good work done<br /><span className="text-primary">should feel easy.</span></h2></div><div className="relative mt-14 grid gap-10 md:grid-cols-3">{[{ n: '01', title: 'Tell us what you need', body: 'Choose a service, add your location and describe the job in a few words.' }, { n: '02', title: 'Choose your fundi', body: 'Compare experience, reviews and availability. Every fundi is identity-checked.' }, { n: '03', title: 'Follow the job', body: 'Send a request, stay in touch and see progress through to completion.' }].map((step, index) => <div key={step.n} className="relative text-center md:text-left"><div className="relative z-10 mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-primary font-display text-2xl text-primary-foreground shadow-[0_10px_22px_rgba(28,170,88,.2)] md:mx-0">{step.n}</div><h3 className="mt-6 text-lg font-bold">{step.title}</h3><p className="mx-auto mt-2 max-w-[260px] text-sm leading-6 text-muted-foreground md:mx-0">{step.body}</p>{index < 2 && <div className="step-line" />}</div>)}</div></section>
    <section className="container-wide pb-20 md:pb-28"><div className="overflow-hidden rounded-[2rem] bg-[#174b37] px-7 py-10 text-white md:px-14 md:py-14"><div className="grid items-center gap-10 md:grid-cols-[1fr_auto]"><div><p className="mb-3 text-xs font-bold uppercase tracking-[.18em] text-[#a9e2bb]">For every home and business</p><h2 className="font-display max-w-xl text-4xl leading-[1] tracking-[-.045em] md:text-5xl">The right hands make all the difference.</h2><p className="mt-5 max-w-lg text-sm leading-6 text-white/70">From a leaking tap to a full home refresh, SkillLink connects you with people who know their craft and care about the result.</p></div><ButtonLink href="/search" variant="primary">Find a Fundi</ButtonLink></div></div></section>
    <section className="bg-[#fbf1df] py-20 md:py-24"><div className="container-wide grid gap-12 md:grid-cols-[.8fr_1.2fr] md:items-center"><div><SectionEyebrow>For fundis</SectionEyebrow><h2 className="font-display max-w-md text-4xl leading-[1.02] tracking-[-.05em] md:text-5xl">Let your good work find its people.</h2><p className="mt-5 max-w-md leading-7 text-muted-foreground">Build a trusted profile, meet customers near you and spend less time looking for your next job.</p><ButtonLink href="/auth?role=fundi&mode=signup" variant="outline">Become a Fundi</ButtonLink></div><div className="grid gap-3 sm:grid-cols-2"><div className="rounded-2xl border border-[#ead8bb] bg-white/70 p-5"><BriefcaseBusiness className="text-primary" size={22} /><h3 className="mt-7 font-bold">More of the work you want</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Showcase your services and let the right customers come to you.</p></div><div className="mt-8 rounded-2xl border border-[#ead8bb] bg-white/70 p-5 sm:mt-0"><ShieldCheck className="text-primary" size={22} /><h3 className="mt-7 font-bold">A profile people trust</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Your experience and reviews do the talking before you arrive.</p></div></div></div></section>
    <section className="container-wide py-20 md:py-28"><div className="grid gap-12 md:grid-cols-[.8fr_1.2fr]"><div><SectionEyebrow>Questions, answered</SectionEyebrow><h2 className="font-display text-4xl tracking-[-.05em] md:text-5xl">Good to know.</h2><p className="mt-4 max-w-sm text-sm leading-6 text-muted-foreground">A few things customers and fundis ask us most.</p></div><div className="divide-y divide-border border-y border-border">{[{ q: 'How are fundis verified?', a: 'Every fundi on SkillLink goes through an identity check before they can accept work. Profiles also include reviews from real customers.' }, { q: 'How do I request a job?', a: 'Search for a service near you, compare fundis, then send a short job request. You can follow each update from your dashboard.' }, { q: 'Is SkillLink available outside Nairobi?', a: 'We are growing across Kenya. Search your location to see the fundis currently serving your area.' }, { q: 'I am a fundi. How do I join?', a: 'Create a fundi account, add your services and service area, and start building your profile for customers nearby.' }].map((item, index) => <div key={item.q} className="py-5"><button className="flex w-full items-center justify-between gap-4 text-left font-semibold" onClick={() => setFaq(faq === index ? null : index)} data-testid={`button-faq-${index}`}>{item.q}<ChevronDown size={18} className={`shrink-0 text-muted-foreground transition-transform ${faq === index ? 'rotate-180' : ''}`} /></button>{faq === index && <p className="mt-3 max-w-2xl pr-8 text-sm leading-6 text-muted-foreground">{item.a}</p>}</div>)}</div></div></section>
    <section className="container-wide pb-24 text-center"><div className="rounded-[2rem] border border-border bg-card px-6 py-12 shadow-[var(--shadow-card)] md:px-12"><Sparkles className="mx-auto text-accent" size={25} /><h2 className="mt-4 font-display text-4xl tracking-[-.05em] md:text-5xl">Ready to get it sorted?</h2><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-muted-foreground">Find a trusted fundi for your next job, or join the network and let customers find you.</p><div className="mt-7 flex flex-wrap justify-center gap-3"><ButtonLink href="/search">Find a Fundi</ButtonLink><ButtonLink href="/auth?mode=signup&role=fundi" variant="outline">Join as a Fundi</ButtonLink></div></div></section>
  </Shell>;
}

function FundiCard({ fundi }: { fundi: Fundi }) { return <Link href={`/fundi/${fundi.id}`} className="group block rounded-2xl border border-border bg-card p-5 transition-all hover:-translate-y-1 hover:border-primary/40 hover:shadow-[var(--shadow-card)]" data-testid={`card-fundi-${fundi.id}`}><div className="flex items-start justify-between gap-3"><Avatar initials={fundi.initials} accent={fundi.accent} /><span className={`rounded-full px-2.5 py-1 text-[11px] font-semibold ${fundi.available ? 'bg-secondary text-primary' : 'bg-muted text-muted-foreground'}`}>{fundi.available ? 'Available' : 'Busy'}</span></div><div className="mt-4"><div className="flex items-center gap-2"><h3 className="font-bold">{fundi.name}</h3>{fundi.verified && <ShieldCheck size={15} className="text-primary" />}</div><p className="mt-1 text-sm text-muted-foreground">{fundi.service}</p><div className="mt-4 flex items-center justify-between text-xs"><Rating rating={fundi.rating} count={fundi.reviewCount} /><span className="flex items-center gap-1 text-muted-foreground"><MapPin size={13} />{fundi.location.split(',')[0]}</span></div></div><div className="mt-5 flex items-center justify-between border-t border-border pt-4 text-xs font-semibold text-primary"><span>{fundi.experience} experience</span><ArrowRight size={15} className="transition-transform group-hover:translate-x-1" /></div></Link>; }

export function SearchPage() {
  const query = new URLSearchParams(typeof window !== 'undefined' ? window.location.search : '');
  const [service, setService] = useState(query.get('service') ?? '');
  const [location, setLocationFilter] = useState(query.get('location') ?? '');
  const [onlineOnly, setOnlineOnly] = useState(false);
  const allFundis = getFundis();
  const results = useMemo(() => allFundis.filter((fundi) => (!service || fundi.services.some((item) => item.toLowerCase() === service.toLowerCase())) && (!location || fundi.location.toLowerCase().includes(location.toLowerCase())) && (!onlineOnly || fundi.available)), [allFundis, location, onlineOnly, service]);
  return <Shell><section className="bg-[#f4f8f3] py-12 md:py-16"><div className="container-wide"><SectionEyebrow>Find your match</SectionEyebrow><h1 className="font-display text-5xl tracking-[-.055em] md:text-6xl">Find a fundi near you.</h1><p className="mt-4 max-w-lg text-muted-foreground">Browse trusted, identity-checked professionals for the jobs that matter.</p><div className="mt-8"><SearchBar compact /></div></div></section><section className="container-wide py-12"><div className="flex flex-col gap-6 md:flex-row md:items-end md:justify-between"><div><p className="text-sm font-semibold">{results.length} {results.length === 1 ? 'fundi' : 'fundis'} found{service ? ` for ${service}` : ''}</p><p className="mt-1 text-sm text-muted-foreground">{location ? `Serving ${location}` : 'Showing trusted professionals across Nairobi'}</p></div><div className="flex flex-wrap gap-2"><select value={service} onChange={(e) => setService(e.target.value)} className="rounded-full border border-border bg-card px-4 py-2.5 text-sm outline-none" data-testid="filter-service"><option value="">All services</option>{services.map((item) => <option key={item.name}>{item.name}</option>)}</select><label className={`flex cursor-pointer items-center gap-2 rounded-full border px-4 py-2.5 text-sm ${onlineOnly ? 'border-primary bg-secondary text-primary' : 'border-border bg-card'}`}><input type="checkbox" checked={onlineOnly} onChange={(e) => setOnlineOnly(e.target.checked)} className="accent-primary" data-testid="checkbox-available" />Available now</label></div></div>{results.length > 0 ? <div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{results.map((fundi) => <FundiCard key={fundi.id} fundi={fundi} />)}</div> : <div className="mt-8 rounded-[2rem] border border-dashed border-border bg-card px-6 py-16 text-center"><span className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-secondary text-primary"><Search size={23} /></span><h2 className="mt-5 font-display text-3xl tracking-[-.04em]">No fundis just yet</h2><p className="mx-auto mt-2 max-w-sm text-sm leading-6 text-muted-foreground">Try a nearby location or another service. We are adding trusted professionals every week.</p><button className="mt-6 rounded-full bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground" onClick={() => { setService(''); setLocationFilter(''); setOnlineOnly(false); }} data-testid="button-clear-filters">Clear filters</button></div>}</section></Shell>;
}

export function HowItWorksPage() {
  return <Shell><section className="container-wide py-16 md:py-24"><div className="max-w-3xl"><SectionEyebrow>How SkillLink works</SectionEyebrow><h1 className="font-display text-5xl leading-[.96] tracking-[-.06em] md:text-7xl">Less chasing.<br /><span className="text-primary">More getting done.</span></h1><p className="mt-6 max-w-xl text-lg leading-8 text-muted-foreground">SkillLink brings customers and trusted fundis together, then keeps everyone in the loop until the job is finished.</p></div><div className="mt-16 grid gap-4 md:grid-cols-3">{[{ icon: Search, title: 'Search with confidence', body: 'Start with the service and location. See experience, reviews and availability in one place.' }, { icon: MessageCircle, title: 'Make a clear request', body: 'Tell your fundi what is needed, where and when. No more guessing before the first visit.' }, { icon: CheckCircle2, title: 'Follow it through', body: 'Get status updates from request to completion and keep the conversation in one thread.' }].map((item, index) => <div key={item.title} className="rounded-3xl border border-border bg-card p-6 shadow-[var(--shadow-card)]"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-secondary text-primary"><item.icon size={21} /></span><p className="mt-8 text-xs font-bold uppercase tracking-[.16em] text-muted-foreground">0{index + 1}</p><h2 className="mt-2 text-xl font-bold">{item.title}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{item.body}</p></div>)}</div></section><section className="border-y border-border/70 bg-[#f4f8f3] py-20" id="fundis"><div className="container-wide grid gap-12 md:grid-cols-2"><div><SectionEyebrow>For customers</SectionEyebrow><h2 className="font-display text-4xl tracking-[-.05em] md:text-5xl">A better way to book help.</h2><div className="mt-8 space-y-5">{['See who is available before you ask', 'Choose based on real experience and reviews', 'Keep job updates and details in one place', 'Know who is coming to your home'].map((item) => <p className="flex items-center gap-3 text-sm font-medium" key={item}><span className="grid h-6 w-6 place-items-center rounded-full bg-primary text-white"><Check size={14} /></span>{item}</p>)}</div><ButtonLink href="/search" variant="outline">Find a Fundi</ButtonLink></div><div className="rounded-[2rem] bg-[#174b37] p-8 text-white md:p-10"><SectionEyebrow>For fundis</SectionEyebrow><h2 className="font-display text-4xl tracking-[-.05em]">Your craft deserves a trusted place to grow.</h2><p className="mt-5 leading-7 text-white/70">SkillLink helps skilled professionals build a profile, reach customers nearby and grow through the quality of their work.</p><div className="mt-8 space-y-4 border-t border-white/15 pt-6">{['Show your services and experience', 'Receive relevant job requests', 'Build your reputation with reviews'].map((item) => <p className="flex items-center gap-3 text-sm font-medium" key={item}><CheckCircle2 className="text-[#a9e2bb]" size={18} />{item}</p>)}</div><ButtonLink href="/auth?role=fundi&mode=signup" variant="primary">Become a Fundi</ButtonLink></div></div></section><section className="container-wide py-20 text-center"><SectionEyebrow>Built for Kenya</SectionEyebrow><h2 className="mx-auto max-w-2xl font-display text-4xl tracking-[-.05em] md:text-5xl">Good work is everywhere.<br /><span className="text-primary">Now it is easier to find.</span></h2><p className="mx-auto mt-5 max-w-lg text-sm leading-6 text-muted-foreground">From Nairobi neighborhoods to the towns beyond, we are building a marketplace around the fundis who keep daily life moving.</p></section></Shell>;
}

export function AuthPage() {
  const params = new URLSearchParams(typeof window !== 'undefined' ? window.location.search : '');
  const [mode, setMode] = useState<'signin' | 'signup'>(params.get('mode') === 'signup' ? 'signup' : 'signin');
  const [role, setRole] = useState<Role>(params.get('role') === 'fundi' ? 'fundi' : 'customer');
  const [name, setName] = useState('');
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');
  const [, setLocation] = useLocation();
  function submit(event: FormEvent) { event.preventDefault(); setError(''); if (!email.includes('@')) return setError('Enter a valid email address.'); if (password.length < 6) return setError('Your password should be at least 6 characters.'); if (mode === 'signup' && name.trim().length < 2) return setError('Tell us your name to get started.'); const users = getUsers(); const existing = users.find((user) => user.email.toLowerCase() === email.toLowerCase()); const user: User = existing ?? { id: role === 'customer' ? 'demo-customer' : `u${Date.now()}`, name: name.trim() || email.split('@')[0], email, role, location: 'Nairobi', available: role === 'fundi' }; saveUsers(existing ? users : [...users, user]); saveSession(user); setLocation('/dashboard'); }
  return <div className="min-h-[calc(100dvh-70px)] bg-[#f4f8f3]"><div className="container-wide grid min-h-[calc(100dvh-70px)] items-center gap-12 py-12 md:grid-cols-[.8fr_1.2fr] md:py-20"><div className="hidden md:block"><Link href="/" className="mb-14 inline-flex items-center gap-2 text-sm font-semibold text-muted-foreground hover:text-primary"><ArrowLeft size={16} />Back to home</Link><SectionEyebrow>Welcome to SkillLink</SectionEyebrow><h1 className="font-display max-w-md text-6xl leading-[.94] tracking-[-.06em]">Good work starts with <span className="text-primary">trust.</span></h1><p className="mt-6 max-w-sm leading-7 text-muted-foreground">Find a trusted fundi for your next job, or join the network and let customers find you.</p><div className="mt-10 flex items-center gap-3 text-xs text-muted-foreground"><span className="grid h-8 w-8 place-items-center rounded-full bg-secondary text-primary"><ShieldCheck size={16} /></span>Identity-checked professionals across Kenya</div></div><div className="mx-auto w-full max-w-[470px] rounded-[2rem] border border-border bg-card p-6 shadow-[var(--shadow-soft)] sm:p-9"><div className="md:hidden"><Link href="/" className="mb-7 inline-flex items-center gap-2 text-sm font-semibold text-muted-foreground"><ArrowLeft size={16} />Back to home</Link></div><div className="flex rounded-xl bg-muted p-1"><button className={`flex-1 rounded-lg py-2.5 text-sm font-semibold ${mode === 'signin' ? 'bg-card shadow-sm' : 'text-muted-foreground'}`} onClick={() => setMode('signin')} data-testid="button-sign-in-tab">Sign in</button><button className={`flex-1 rounded-lg py-2.5 text-sm font-semibold ${mode === 'signup' ? 'bg-card shadow-sm' : 'text-muted-foreground'}`} onClick={() => setMode('signup')} data-testid="button-sign-up-tab">Create account</button></div><div className="mt-8"><h2 className="font-display text-3xl tracking-[-.04em]">{mode === 'signin' ? 'Welcome back.' : 'Let’s get started.'}</h2><p className="mt-2 text-sm text-muted-foreground">{mode === 'signin' ? 'Sign in to follow your jobs.' : 'Choose how you will use SkillLink.'}</p></div>{mode === 'signup' && <div className="mt-6 grid grid-cols-2 gap-2"><button onClick={() => setRole('customer')} className={`rounded-xl border p-3 text-left ${role === 'customer' ? 'border-primary bg-secondary' : 'border-border'}`} data-testid="button-role-customer"><span className="text-sm font-bold">I need a fundi</span><span className="mt-1 block text-xs text-muted-foreground">For customers</span></button><button onClick={() => setRole('fundi')} className={`rounded-xl border p-3 text-left ${role === 'fundi' ? 'border-primary bg-secondary' : 'border-border'}`} data-testid="button-role-fundi"><span className="text-sm font-bold">I am a fundi</span><span className="mt-1 block text-xs text-muted-foreground">For professionals</span></button></div>}<form className="mt-6 space-y-4" onSubmit={submit}><label className="block text-sm font-semibold">{mode === 'signup' && 'Your name'}{mode === 'signup' && <input value={name} onChange={(e) => setName(e.target.value)} className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 font-normal outline-none ring-primary/20 transition focus:ring-4" placeholder={role === 'fundi' ? 'e.g. Moses Kariuki' : 'e.g. Akinyi Otieno'} data-testid="input-name" />}</label><label className="block text-sm font-semibold">Email address<input value={email} onChange={(e) => setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 font-normal outline-none transition focus:ring-4 focus:ring-primary/20" type="email" placeholder="you@example.com" data-testid="input-email" /></label><label className="block text-sm font-semibold">Password<input value={password} onChange={(e) => setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 font-normal outline-none transition focus:ring-4 focus:ring-primary/20" type="password" placeholder="At least 6 characters" data-testid="input-password" /></label>{error && <p className="rounded-xl bg-[#fff1ed] px-4 py-3 text-sm font-medium text-[#b04e3e]" data-testid="text-auth-error">{error}</p>}<button className="flex w-full items-center justify-center gap-2 rounded-xl bg-primary py-3.5 text-sm font-bold text-primary-foreground shadow-[0_8px_20px_rgba(28,170,88,.18)] transition-transform hover:-translate-y-0.5" type="submit" data-testid="button-auth-submit">{mode === 'signin' ? 'Sign in' : 'Create account'}<ArrowRight size={16} /></button></form><p className="mt-5 text-center text-xs leading-5 text-muted-foreground">By continuing, you agree to use SkillLink respectfully and honestly.</p></div></div></div>;
}

function StatusPill({ status }: { status: JobStatus }) { const styles: Record<JobStatus, string> = { Requested: 'bg-[#fff4dc] text-[#996212]', Accepted: 'bg-[#e5f0ff] text-[#356598]', 'In progress': 'bg-secondary text-primary', Completed: 'bg-[#e9e9e3] text-[#53604e]' }; return <span className={`rounded-full px-2.5 py-1 text-[11px] font-bold ${styles[status]}`}>{status}</span>; }
function JobCard({ job, fundiMode, onStatus }: { job: Job; fundiMode?: boolean; onStatus?: (status: JobStatus) => void }) { const next: JobStatus | null = job.status === 'Requested' ? 'Accepted' : job.status === 'Accepted' ? 'In progress' : job.status === 'In progress' ? 'Completed' : null; return <div className="rounded-2xl border border-border bg-card p-5 shadow-[var(--shadow-card)]"><div className="flex items-start justify-between gap-4"><div className="flex gap-3"><span className="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-secondary text-primary"><Wrench size={18} /></span><div><p className="font-bold">{job.service}</p><p className="mt-1 flex items-center gap-1 text-xs text-muted-foreground"><MapPin size={12} />{job.location}</p></div></div><StatusPill status={job.status} /></div><p className="mt-4 text-sm leading-6 text-muted-foreground">{job.details}</p>{(job.preferredDate || job.preferredTime) && <p className="mt-3 flex items-center gap-1.5 text-xs font-medium text-muted-foreground"><Clock3 size={13} />Preferred: {[job.preferredDate, job.preferredTime].filter(Boolean).join(' · ')}</p>}{!fundiMode && <div className="mt-5 border-t border-border pt-4"><p className="mb-3 text-[10px] font-bold uppercase tracking-[.14em] text-muted-foreground">Status history</p><div className="space-y-3">{job.history.map((item, index) => <div className="flex gap-3 text-xs" key={`${item.status}-${index}`}><span className={`mt-0.5 h-2 w-2 shrink-0 rounded-full ${item.status === job.status ? 'bg-primary' : 'bg-border'}`} /><span className="min-w-0"><strong>{item.status}</strong><span className="ml-2 text-muted-foreground">{item.date}</span><span className="mt-0.5 block text-muted-foreground">{item.note}</span></span></div>)}</div></div>}<div className="mt-5 flex items-center justify-between border-t border-border pt-4"><div className="flex items-center gap-2 text-xs text-muted-foreground">{fundiMode ? <><Avatar initials={job.customerName.split(' ').map((n) => n[0]).join('')} size="sm" /><span>Requested by <strong className="text-foreground">{job.customerName}</strong></span></> : <><span>Fundi</span><strong className="text-foreground">{job.fundiName ?? 'Finding a match'}</strong></>}</div>{next && onStatus && <button onClick={() => onStatus(next)} className="rounded-full border border-primary px-3 py-2 text-xs font-bold text-primary hover:bg-secondary" data-testid={`button-status-${job.id}`}>{next === 'Accepted' ? 'Accept job' : `Mark ${next.toLowerCase()}`}</button>}</div></div>; }

function RequestModal({ onClose, fundi }: { onClose: () => void; fundi?: Fundi }) { const session = getSession(); const [, setLocation] = useLocation(); const [service, setService] = useState(fundi?.service ?? ''); const [location, setLocalLocation] = useState(session?.location ?? ''); const [details, setDetails] = useState(''); const [preferredDate, setPreferredDate] = useState(''); const [preferredTime, setPreferredTime] = useState(''); const [error, setError] = useState(''); function submit(event: FormEvent) { event.preventDefault(); if (!service || !location || details.trim().length < 10) return setError('Add a service, location and a little more detail about the job.'); if (!session) return setLocation('/auth?mode=signup'); createJob({ customerId: session.id, customerName: session.name, service, location, details, preferredDate: preferredDate.trim() || undefined, preferredTime: preferredTime.trim() || undefined, fundiId: fundi?.id, fundiName: fundi?.name }); onClose(); } return <div className="fixed inset-0 z-50 grid place-items-center bg-[#173529]/45 p-4" role="dialog"><div className="max-h-[92dvh] w-full max-w-lg overflow-y-auto rounded-3xl border border-border bg-card p-6 shadow-2xl sm:p-8"><div className="flex items-start justify-between"><div><SectionEyebrow>New request</SectionEyebrow><h2 className="font-display text-3xl tracking-[-.04em]">{fundi ? `Request ${fundi.name}` : 'What needs doing?'}</h2><p className="mt-2 text-sm text-muted-foreground">A clear brief helps the right fundi respond.</p></div><button onClick={onClose} className="rounded-full p-2 hover:bg-muted" aria-label="Close request form" data-testid="button-close-request"><X size={18} /></button></div><form className="mt-7 space-y-4" onSubmit={submit}><label className="block text-sm font-semibold">Service<select value={service} onChange={(e) => setService(e.target.value)} className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 text-sm font-normal outline-none" data-testid="input-request-service"><option value="">Choose a service</option>{services.map((item) => <option key={item.name}>{item.name}</option>)}</select></label><label className="block text-sm font-semibold">Where is the job?<input value={location} onChange={(e) => setLocalLocation(e.target.value)} placeholder="e.g. Kilimani, Nairobi" className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 font-normal outline-none focus:ring-4 focus:ring-primary/20" data-testid="input-request-location" /></label><label className="block text-sm font-semibold">Tell us about the job<textarea value={details} onChange={(e) => setDetails(e.target.value)} placeholder="What is happening? What would you like fixed?" rows={4} className="mt-2 w-full resize-none rounded-xl border border-input bg-background px-4 py-3 font-normal outline-none focus:ring-4 focus:ring-primary/20" data-testid="input-request-details" /></label><div className="grid gap-3 sm:grid-cols-2"><label className="block text-sm font-semibold">Preferred date<input type="text" value={preferredDate} onChange={(e) => setPreferredDate(e.target.value)} placeholder="e.g. Friday, 4 July" className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 text-sm font-normal outline-none focus:ring-4 focus:ring-primary/20" data-testid="input-request-date" /></label><label className="block text-sm font-semibold">Preferred time<input type="text" value={preferredTime} onChange={(e) => setPreferredTime(e.target.value)} placeholder="e.g. 2:00 PM" className="mt-2 w-full rounded-xl border border-input bg-background px-4 py-3 text-sm font-normal outline-none focus:ring-4 focus:ring-primary/20" data-testid="input-request-time" /></label></div>{error && <p className="text-sm font-medium text-[#b04e3e]" data-testid="text-request-error">{error}</p>}<button type="submit" className="w-full rounded-xl bg-primary py-3.5 text-sm font-bold text-primary-foreground" data-testid="button-submit-request">Send request</button></form></div></div>; }

export function DashboardPage() {
  const [, setLocation] = useLocation(); const session = getSession(); const [jobs, setJobs] = useState(getJobs()); const [modal, setModal] = useState(Boolean(new URLSearchParams(typeof window !== 'undefined' ? window.location.search : '').get('request'))); const fundiMode = session?.role === 'fundi'; const myFundi = fundiMode ? getFundis().find((fundi) => fundi.name === session?.name) ?? getFundis()[0] : null; const ownJobs = jobs.filter((job) => fundiMode ? job.fundiName === session?.name || job.fundiId === myFundi?.id : job.customerId === session?.id || (session?.id === 'demo-customer' && job.customerId === 'demo-customer')); if (!session) return <Shell><section className="container-wide grid min-h-[60vh] place-items-center py-20 text-center"><span className="grid h-16 w-16 place-items-center rounded-2xl bg-secondary text-primary"><ShieldCheck size={28} /></span><h1 className="mt-6 font-display text-4xl tracking-[-.05em]">Your dashboard is waiting.</h1><p className="mt-3 max-w-sm text-sm leading-6 text-muted-foreground">Sign in to create requests, follow your jobs and keep everything in one place.</p><ButtonLink href="/auth?mode=signup">Create an account</ButtonLink></section></Shell>; function changeStatus(id: string, status: JobStatus) { updateJobStatus(id, status); setJobs(getJobs()); } function logout() { saveSession(null); setLocation('/'); } return <Shell><section className="bg-[#f4f8f3] py-12 md:py-16"><div className="container-wide flex flex-col gap-6 sm:flex-row sm:items-end sm:justify-between"><div><SectionEyebrow>{fundiMode ? 'Fundi workspace' : 'Your workspace'}</SectionEyebrow><h1 className="font-display text-5xl tracking-[-.055em]">Good morning, {session.name.split(' ')[0]}.</h1><p className="mt-3 text-sm text-muted-foreground">{fundiMode ? 'Keep your availability current and stay close to incoming work.' : 'Everything for your jobs, in one calm place.'}</p></div><div className="flex gap-2"><button onClick={logout} className="rounded-full border border-border bg-card px-4 py-2.5 text-sm font-semibold text-muted-foreground hover:text-foreground" data-testid="button-log-out">Log out</button>{!fundiMode && <button onClick={() => setModal(true)} className="rounded-full bg-primary px-4 py-2.5 text-sm font-bold text-primary-foreground" data-testid="button-new-request">New request</button>}</div></div></section><section className="container-wide py-10 md:py-14">{fundiMode ? <><div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><h2 className="font-display text-3xl tracking-[-.04em]">Incoming jobs</h2><p className="mt-1 text-sm text-muted-foreground">Requests matched to your services and area.</p></div><div className="flex items-center gap-3 rounded-2xl border border-border bg-card px-4 py-3"><span className={`h-2.5 w-2.5 rounded-full ${myFundi?.available ? 'bg-primary' : 'bg-muted-foreground'}`} /><span className="text-sm font-semibold">{myFundi?.available ? 'Available for work' : 'Not available'}</span><button onClick={() => { if (myFundi) { toggleFundiAvailability(myFundi.id); setJobs([...getJobs()]); } }} className="ml-2 rounded-full border border-border px-3 py-1.5 text-xs font-bold" data-testid="button-toggle-availability">{myFundi?.available ? 'Pause' : 'Go available'}</button></div></div></> : <div className="mb-8 grid gap-3 sm:grid-cols-3"><div className="rounded-2xl border border-border bg-card p-5"><p className="text-xs font-bold uppercase tracking-[.14em] text-muted-foreground">Active jobs</p><p className="mt-3 font-display text-4xl">{ownJobs.filter((job) => job.status !== 'Completed').length}</p></div><div className="rounded-2xl border border-border bg-card p-5"><p className="text-xs font-bold uppercase tracking-[.14em] text-muted-foreground">Completed</p><p className="mt-3 font-display text-4xl">{ownJobs.filter((job) => job.status === 'Completed').length}</p></div><div className="rounded-2xl border border-border bg-card p-5"><p className="text-xs font-bold uppercase tracking-[.14em] text-muted-foreground">Trusted network</p><p className="mt-3 text-lg font-bold text-primary">Always growing</p></div></div>}{ownJobs.length ? <div className="grid max-w-3xl gap-4">{ownJobs.map((job) => <JobCard key={job.id} job={job} fundiMode={fundiMode} onStatus={fundiMode ? (status) => changeStatus(job.id, status) : undefined} />)}</div> : <div className="rounded-3xl border border-dashed border-border bg-card px-6 py-16 text-center"><Clock3 className="mx-auto text-primary" size={28} /><h2 className="mt-4 font-display text-3xl">No jobs here yet</h2><p className="mx-auto mt-2 max-w-sm text-sm leading-6 text-muted-foreground">{fundiMode ? 'When a customer sends a request that matches your services, it will show up here.' : 'Your next job starts with a simple request.'}</p>{!fundiMode && <button onClick={() => setModal(true)} className="mt-6 rounded-full bg-primary px-5 py-3 text-sm font-bold text-primary-foreground">Create a request</button>}</div>}</section>{modal && !fundiMode && <RequestModal onClose={() => { setModal(false); setJobs(getJobs()); }} />}</Shell>;
}

export function FundiProfilePage() {
  const { id } = useParams<{ id: string }>(); const fundi = getFundis().find((item) => item.id === id); const [modal, setModal] = useState(false); if (!fundi) return <NotFoundPage />; return <Shell><section className="bg-[#f4f8f3] py-12 md:py-20"><div className="container-wide"><Link href="/search" className="mb-10 inline-flex items-center gap-2 text-sm font-semibold text-muted-foreground hover:text-primary"><ArrowLeft size={16} />Back to search</Link><div className="grid gap-10 md:grid-cols-[1fr_1.3fr] md:items-end"><div><Avatar initials={fundi.initials} accent={fundi.accent} size="lg" /><div className="mt-5 flex items-center gap-2"><VerifiedBadge /><span className={`text-xs font-semibold ${fundi.available ? 'text-primary' : 'text-muted-foreground'}`}>{fundi.available ? 'Available now' : 'Currently busy'}</span></div><h1 className="mt-4 font-display text-5xl tracking-[-.055em] md:text-6xl">{fundi.name}</h1><p className="mt-2 text-lg text-muted-foreground">{fundi.service}</p><div className="mt-5 flex flex-wrap gap-4"><Rating rating={fundi.rating} count={fundi.reviewCount} /><span className="flex items-center gap-1.5 text-sm text-muted-foreground"><MapPin size={15} />{fundi.location}</span><span className="flex items-center gap-1.5 text-sm text-muted-foreground"><Clock3 size={15} />{fundi.experience}</span></div></div><div className="rounded-3xl border border-border bg-card p-6 shadow-[var(--shadow-card)]"><p className="text-xs font-bold uppercase tracking-[.15em] text-muted-foreground">Looking for help?</p><h2 className="mt-2 font-display text-3xl tracking-[-.04em]">Tell {fundi.name.split(' ')[0]} what you need.</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Send a request and agree on the details before work starts.</p><button onClick={() => setModal(true)} className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-primary py-3.5 text-sm font-bold text-primary-foreground" data-testid="button-request-fundi">Request this fundi <ArrowRight size={16} /></button></div></div></div></section><section className="container-wide grid gap-12 py-14 md:grid-cols-[1.2fr_.8fr] md:py-20"><div><h2 className="font-display text-3xl tracking-[-.04em]">About the work</h2><p className="mt-4 max-w-2xl text-[16px] leading-8 text-muted-foreground">{fundi.bio}</p><h2 className="mt-12 font-display text-3xl tracking-[-.04em]">Services</h2><div className="mt-5 flex flex-wrap gap-2">{fundi.services.map((service) => <span key={service} className="rounded-full border border-border bg-card px-4 py-2 text-sm font-medium">{service}</span>)}</div><h2 className="mt-12 font-display text-3xl tracking-[-.04em]">What customers say</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{[{ name: 'Janet M.', quote: 'Clear communication and very neat work. I knew what to expect at every step.' }, { name: 'Peter O.', quote: 'Arrived when he said he would and fixed the issue properly. Would recommend.' }].map((review) => <div className="rounded-2xl border border-border bg-card p-5" key={review.name}><Rating rating={5} /><p className="mt-3 text-sm leading-6 text-muted-foreground">“{review.quote}”</p><p className="mt-4 text-xs font-bold">{review.name}</p></div>)}</div></div><aside className="h-fit rounded-3xl border border-border bg-[#fbf1df] p-6"><p className="text-xs font-bold uppercase tracking-[.15em] text-muted-foreground">At a glance</p><div className="mt-6 space-y-5">{[['Experience', fundi.experience], ['Service area', fundi.location], ['Completed jobs', `${fundi.reviewCount + 18}`], ['Response', 'Usually within a few hours']].map(([label, value]) => <div className="flex items-center justify-between gap-4 border-b border-[#ead8bb] pb-4 text-sm" key={label}><span className="text-muted-foreground">{label}</span><strong className="text-right">{value}</strong></div>)}</div><div className="mt-6 flex items-center gap-2 text-xs text-muted-foreground"><ShieldCheck size={15} className="text-primary" />Profile verified by SkillLink</div></aside></section>{modal && <RequestModal fundi={fundi} onClose={() => setModal(false)} />}</Shell>;
}

export function NotFoundPage() { return <Shell><section className="container-wide grid min-h-[65vh] place-items-center py-20 text-center"><span className="font-display text-8xl text-primary/20">404</span><h1 className="mt-2 font-display text-4xl tracking-[-.05em]">This page wandered off.</h1><p className="mt-3 max-w-sm text-sm leading-6 text-muted-foreground">The link may be out of date, but there is good work waiting back at home.</p><ButtonLink href="/">Back to home</ButtonLink></section></Shell>; }